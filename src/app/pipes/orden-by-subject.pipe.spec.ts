import { OrdenBySubjectPipe } from './orden-by-subject.pipe';

describe('OrdenBySubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenBySubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
