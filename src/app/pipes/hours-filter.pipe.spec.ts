import { HoursFilterPipe } from './hours-filter.pipe';

describe('HoursFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
